# ubuntu-wsl2-systemd-scriptxport GCM_CREDENTIAL_STORE="gpg"
# if [ -z "$(grep -Fxq 'Clear Linux OS' /etc/os-release)" ]; then
#	# code if found
#	GPG_TTY=$(tty)
# 	export GPG_TTY
# fi
export GPG_TTY=$(tty)
